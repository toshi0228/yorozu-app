export default class Reviews {

    constructor({ id, sourceId, targetId, score}) {
        this.id = id
        this.sourceId = sourceId
        this.targetId = targetId
        this.score = score
        }
    }