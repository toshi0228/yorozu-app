
export default class PlanSubscription {

    constructor({ id, sourceId, targetId, paymentPrice}) {
        this.id = id
        this.sourceId = sourceId
        this.targetId = targetId
        this.paymentPrice = paymentPrice
        }
    }